name: VITA - Nightly

on:
  push:
    branches:
      - master
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  repository_dispatch:
    types: [run_build]

jobs:
  build:
    runs-on: ubuntu-latest
    container: vitasdk/buildscripts
    env:
      SSHPASS: ${{ secrets.SSHPASS }}
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        apk add build-base zip bash git imagemagick p7zip util-linux tar bsdtar sshpass curl
    - name: Prepare Build
      run: |
        addgroup -S vitasdk && adduser -S vitasdk -G vitasdk
        chown -R vitasdk:vitasdk .
        su vitasdk -pc 'bash ./travis_prepare.sh' -s /bin/sh
    - name: Build packages
      run: |
        su vitasdk -pc 'bash ./travis_build.sh' -s /bin/sh
        su vitasdk -pc 'bash ./travis_upload.sh' -s /bin/sh