pkgname=vitaGL
pkgver=9999
pkgrel=1
url="https://github.com/Rinnegatamante/vitaGL"
source=("git://github.com/Rinnegatamante/vitaGL.git")
sha256sums=('SKIP')

build() {
  cd $pkgname
  make HAVE_SBRK=1 NO_DEBUG=1 -j$(nproc)
}

package () {
  cd $pkgname
  make DESTDIR=$pkgdir install
}
